<div class="container">
  <div class="row main-row">
    <div class="col-12 mx-auto">
      <div class="row justify-content-center">
        <h4 class="col-md-6 mb-5 title">投稿作成</h4>
      </div>

      <%= render "user/shared/error_messages", resource: @post %>

      <!--投稿ユーザー情報-->
      <div class="row justify-content-center">
        <div class="col-md-6">
          <% if @user %>
            <span><%= image_tag @user.get_profile_image(50, 50) %></span>
            <span class="ml-5"><%= @user.name %></span>
          <% else %>
            投稿を作成できません
          <% end %>
        </div>
      </div>

      <%= form_with model: @post, url: posts_path, local: true do |f| %>

        <!--本文入力-->
        <div class="form-row justify-content-center">
          <div class="form-group col-md-6 mt-5">
            <label for="content">本文</label>
            <%= f.text_field :content, autofocus: true, class: "form-control", style: "height: 100%;" %>
          </div>
        </div>

        <!--画像選択-->
        <div class="form-row justify-content-center">
          <div class="form-group col-md-6 mt-5">
            <label for="image">添付画像</label>
            <%= f.file_field :image, accept: "image/*" %>
          </div>
        </div>

        <!--後日余裕あれば画像プレビュー機能実装-->

        <!--都道府県選択-->
        <div class="form-row justify-content-center">
          <div class="form-group col-md-6 mt-5">
            <label for="prefecture">都道府県</label>
            <%= f.collection_select :prefecture_id, Prefecture.all, :id, :name, { prompt: "選択してください" }, { class: "form-control" } %>
          </div>
        </div>

        <!--タグ追加-->
        <div class="form-row justify-content-center">
          <div class="form-group col-md-6 mt-5">
            <label for="tag_list">タグを追加</label>
            <%= f.text_field :tag_list, value: @post.tag_list.join(','), class: "form-control", data: { role: "tagsinput" } %>
          </div>
        </div>

        <div class="form-row mt-3 justify-content-center">
          <!--キャンセルボタン-->
          <div class="col-auto cancel-btn">
            <%= link_to "戻る", request.referer || posts_path, class: "btn btn-secondary" %>
          </div>
          <!-- 下書き保存ボタン -->
          <div class="col-auto draft-btn">
            <%= f.submit "下書きに保存", name: 'post[post_status]', value: 'draft', class: 'btn btn-md btn-info' %>
          </div>
          <!-- 非公開保存ボタン -->
          <div class="col-auto unpublicized-btn">
            <%= f.submit "非公開に保存", name: 'post[post_status]', value: 'unpublicized', class: 'btn btn-md btn-warning' %>
          </div>
          <!-- 投稿するボタン -->
          <div class="col-auto submit-btn">
            <%= f.submit "投稿を公開", name: 'post[post_status]', value: 'publicized', class: 'btn btn-md btn-success' %>
          </div>
        </div>

      <% end %>

    </div>
  </div>
</div>
